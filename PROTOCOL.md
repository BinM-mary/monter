# 通信协议帧记录

本文档是上位机与设备通信的协议帧记录入口。后续新增、修改或实现帧处理功能时，必须同步在本文档登记对应的命令、数据定义和处理状态。

## 1. 主机发送帧格式

```text
AA 55 | CMD | SUB_CMD | LEN_H | LEN_L | DATA[0] ... DATA[n-1] | CHECKSUM
```

| 字段 | 长度 | 定义 |
| --- | ---: | --- |
| Break | 2 bytes | 固定帧头：`0xAA 0x55` |
| Cmd | 1 byte | 主命令，取值范围：`0x10` ～ `0x7F` |
| Sub Cmd | 1 byte | 子命令，取值范围：`0x00` ～ `0x7F` |
| Data Length | 2 bytes | 数据区长度，按高字节在前：`LEN_H`、`LEN_L` |
| Data | n bytes | 数据区：`DATA[0]` ～ `DATA[n-1]` |
| Checksum | 1 byte | `Break + CMD + SUB_CMD + LEN_H + LEN_L + DATA` 的 XOR 校验值 |

- 数据区长度：`n = (LEN_H << 8) | LEN_L`。
- 整帧长度：`7 + n` 字节。
- 两个帧头字节参与校验，末尾 `CHECKSUM` 自身不参与校验。

## 2. 从机回应帧格式

```text
55 AA | CMD | SUB_CMD_RESP | LEN_H | LEN_L | DATA[0] ... DATA[n-1] | CHECKSUM
```

| 字段 | 长度 | 定义 |
| --- | ---: | --- |
| Break | 2 bytes | 固定帧头：`0x55 0xAA` |
| Cmd | 1 byte | 主命令，取值范围：`0x10` ～ `0x7F` |
| Sub Cmd Resp | 1 byte | 支持请求时：`请求 SUB_CMD + 0x40`；不支持请求时：`0xDF` |
| Data Length | 2 bytes | 数据区长度，按高字节在前：`LEN_H`、`LEN_L` |
| Data | n bytes | 数据区：`DATA[0]` ～ `DATA[n-1]` |
| Checksum | 1 byte | `Break + CMD + SUB_CMD_RESP + LEN_H + LEN_L + DATA` 的 XOR 校验值 |

- 数据区长度：`n = (LEN_H << 8) | LEN_L`。
- 整帧长度：`7 + n` 字节。
- 两个帧头字节参与校验，末尾 `CHECKSUM` 自身不参与校验。

## 3. 校验算法

初始校验值为 `0x00`，从第一个帧头字节开始依次异或至最后一个数据字节。

```c
uint8_t chk = 0;
for (uint8_t i = 0; i < len; i++)
{
    chk ^= p_buf[i];
}
return chk;
```

发送帧中：

- `p_buf[0]` 是第一个帧头字节。
- `len = 6 + n`，即覆盖两个帧头字节、`CMD`、`SUB_CMD`、`LEN_H`、`LEN_L` 和全部 `DATA`。
- 计算结果写入帧末尾的 `CHECKSUM`。

## 4. 后续帧登记规范

每新增或修改一项协议帧处理时，在下表增加或更新对应记录，并写明数据区的字节定义、方向和当前实现状态。

| 功能 | 方向 | CMD | SUB_CMD | 数据区定义 | 校验 | 上位机处理状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 通用帧编解码框架 | TX / RX | `0x10` ～ `0x7F` | 请求 `0x00` ～ `0x3F`；响应为请求值 `+ 0x40` 或 `0xDF` | 帧构造、分段接收重组、长度与包含帧头的 XOR 校验 | XOR（见第 3 节） | 已实现 |
| 读取初始化步数 | TX `0x10 / 0x00`，RX `0x10 / 0x40` | `0x10` | 请求 `0x00`；响应 `0x40` | 无；成功响应 Data 为 `uint32` 大端 step | XOR（见第 3 节） | 已接入，待展示数据值 |
| 读取固件版本 | TX `0x10 / 0x01`，RX `0x10 / 0x41` | `0x10` | 请求 `0x01`；响应 `0x41` | 无；成功响应 Data 为软件主/次/修订版本 3 字节 | XOR（见第 3 节） | 已接入，待展示数据值 |
| 读取硬件版本 | TX `0x10 / 0x02`，RX `0x10 / 0x42` | `0x10` | 请求 `0x02`；响应 `0x42` | 无；成功响应 Data 为硬件版本索引 1 字节 | XOR（见第 3 节） | 已接入，待展示数据值 |
| 读取电压和温度 | TX `0x10 / 0x03`，RX `0x10 / 0x43` | `0x10` | 请求 `0x03`；响应 `0x43` | 无；成功响应 Data 为 `uint16` 大端 mV 和 `int16` 大端 0.1 ℃ | XOR（见第 3 节） | 已接入，待展示数据值 |

> XOR 以本节为唯一准则：校验范围包含两个帧头字节，排除末尾 `CHECKSUM` 字节。

## 5. 帧处理规则

1. 接收时根据帧方向确认帧头：主机发送帧为 `0xAA 0x55`，从机回应帧为 `0x55 0xAA`。
2. 读取 `CMD`、子命令和两个长度字节后，计算数据区长度 `n`。
3. 收齐 `n` 字节数据和 1 字节校验值后，再进行 XOR 校验。
4. 从机正常回应时，确认 `SUB_CMD_RESP = 请求 SUB_CMD + 0x40`；若值为 `0xDF`，表示该请求不受支持。
5. 只有帧头、长度和校验都正确的帧，才交给对应命令处理逻辑。
6. 初期采用单请求串行策略：同一时刻仅允许一个读取请求在途；收到匹配的成功响应、不支持响应或超时后，才允许发送下一个请求。
7. 收到 `0xDF`、发送失败、响应超时、帧头/长度/XOR 校验失败时，上位机应显示对应失败提示，不更新设备信息展示值。
8. 读取请求超时使用串口设置中已应用的“读取超时(ms)”，默认值为 1000 ms；用户修改并应用串口设置后，后续请求使用新值。

9. 报文输出窗口默认跟随最新收发记录自动滚动到最后一行；用户主动向上查看历史记录时保持当前位置，回到底部后恢复自动跟随。
10. 完整但帧头、长度或 XOR 校验不通过的接收帧，必须从正常命令处理链路中丢弃；同时保留原始帧写入报文日志，类型标记为“错误”，不得用于响应匹配或更新设备信息。
11. 报文输出窗口支持按方向、帧类型、Cmd 和 SubCmd 过滤；多个条件同时选择时按“并且”关系筛选，过滤不删除底层日志，Cmd/SubCmd 选项随当前日志动态更新。

## 6. 协议草案待确认项

- `0x30` 的故障与初始化状态子命令在“命令清单”“故障位”“数据结构”三个页签中存在不同定义，尚未绑定任何业务处理。
- `0x10 / 0x00` 的示例响应为 4 字节初始化步数，而“DeviceInfo”数据结构定义为 5 字节版本与装配信息；需明确它们是同一命令的不同版本，还是拆分为独立子命令。
