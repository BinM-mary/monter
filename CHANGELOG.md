# 修改记录

## 2026-09-17

### 快捷位置读入当前位置

- `OPEN1`、`OPEN2`、`OPEN3` 各增加“读入当前位置”按钮，按钮位于对应预设右侧。
- 点击后将当前步数写入对应预设并保存本机配置，同时刷新该预设的 step 与百分比摘要。

### 快捷位置布局优化

- 将三个独立的“读入当前位置”按钮合并为“记忆当前位置”下拉按钮，可选择保存到 `OPEN1`、`OPEN2` 或 `OPEN3`。
- 在“快捷位置”标题右侧显示当前 step 与百分比，并将统一配置入口移动到右侧“配置”按钮。
- 删除原先位于快捷位置下方的“统一配置 OPEN1 / OPEN2 / OPEN3”按钮；配置内容仍保持同时编辑三个 OPEN 预设。

### 快捷位置下拉菜单启动异常修复

- 移除下拉菜单项对页面根节点的 `x:Reference` 自引用绑定，修复启动时 `XamlParseException` 的循环依赖错误。

### 电机控制增加初始化校准入口

- 在“统一配置 OPEN1 / OPEN2 / OPEN3”和“STOP”之间增加“初始化校准”按钮，复用统一配置按钮的颜色和样式。
- 初始化校准沿用电机动作按钮的禁用状态，待串口连接、初始化步数和对应协议命令接入后再启用。

### 协议长度字段修正

- 协议帧的 Data Length 统一调整为 1 byte，数据区长度范围为 0～255 bytes。
- 当长度为 0 时不附加 Data 字节；帧构造、接收拆帧、报文窗口字段拆分以及长度/XOR 校验索引同步调整。
- XOR 校验范围调整为 `CMD + SUB_CMD + LEN + DATA`，不再包含 `Break` 帧头。
- 初始化步数响应按 `LEN` 指定长度解析为大端无符号整数：`55 AA 10 40 02 10 20 62` 显示为 4128，`LEN=03` 的 `10 10 20` 显示为 1052704。
- 收到 `SUB_CMD=0xDF` 的有效响应时，报文输出窗口类型标记为“不支持”，并保留原有不支持响应处理。

### 报文输出窗口字段细分

- 报文输出窗口按协议拆分显示方向、帧类型、Break、Cmd、SubCmd、Data 长度、Data 和 Checksum，并保留原始完整帧用于核对。
- 增加帧头、长度及 XOR 校验状态提示，校验计算遵循当前协议规则（不包含帧头）。

### 报文输出窗口自动跟随

- 报文新增时，窗口会自动滚动到最新一行；用户主动向上查看历史记录时暂不强制跳转，回到底部后恢复自动跟随。

### 错误帧日志记录

- 完整但校验不通过的接收帧会从正常协议处理链路中丢弃，同时保留原始帧写入报文日志，类型标记为“错误”，便于定位通信问题。

### 报文输出窗口多条件过滤

- 报文窗口增加方向、类型、Cmd 和 SubCmd 过滤器，多个条件同时生效时按“并且”关系筛选。
- 过滤只改变窗口显示，不删除底层日志；Cmd/SubCmd 选项会根据当前日志动态更新，并提供重置筛选入口。

### 通信收发基础框架

- 新增通用帧构造与接收解析：发送帧按 `AA 55` 构造，接收缓冲按 `55 AA` 重组完整响应帧，并校验长度和 XOR。
- 新增串口原始字节收发入口；仅在成功发送或成功解析出完整响应帧后写入共享通信日志。
- 已将四个设备信息读取命令绑定到对应按钮，完成请求发送、响应匹配、单请求串行控制和失败提示；设备数据展示待后续接入。
- 协议确认：XOR 校验按 `CMD + SUB_CMD + LEN + DATA` 计算，读取电压和温度使用 `0x10 / 0x03`，四个设备信息读取命令采用单请求串行收发并在失败时提示用户。

### 读取设备信息交互反馈

- 读取设备信息区域的四个读取按钮点击后，会在右下角显示对应操作提示，并在 1 秒后自动消失；提示期间仍可继续点击其他读取按钮。

### 设备信息显示状态同步

- 串口连接状态发生变化时，初始化步数、退步度数、固件版本、硬件版本、当前电压和当前温度会先清空，不再显示上一次连接设备的残留值。
- 仅在对应读取命令收到帧头、长度、校验和子命令均有效的响应后，更新对应的设备信息；超时、不支持、发送失败或数据长度错误不会覆盖已有值。
- 当前协议尚未确认退步度数的独立读取命令，因此该字段保持为空，待协议补充后再接入对应响应。

### 报文窗口复用与日志保留上限

- 报文输出窗口点击关闭时改为隐藏并保留窗口实例，再次打开直接复用，避免重复创建窗口和重新绑定全部历史报文。
- 报文日志默认保留最近 5000 条，并在窗口中提供“5000 条 / 1 万条”切换；切换到较小上限时自动淘汰最旧记录。
- 日志达到保留上限后只保留最新报文，清空按钮仍可立即清除当前全部日志。
- 修复高频接收报文时窗口复用实例持续触发全量筛选刷新和强制布局的问题；筛选选项改为增量维护，隐藏窗口不执行自动滚动，达到上限时使用增量移除/添加。
- 修复高频接收期间点击“清空”时筛选下拉框短暂变为空值导致的异常；清空过程中会安全恢复为“全部”筛选。

### 报文过滤器级联联动

- 方向、类型、Cmd 和 SubCmd 四个筛选器改为相互联动；每个下拉框只显示在其他已选条件下仍能匹配到的日志值。
- 当筛选组合变化导致原选项失效时，自动恢复为“全部”，避免出现无法匹配或空值筛选状态。

### 级联筛选选中项重置修复

- 问题现象：先选择方向 `RX`，再选择任意 SubCmd 时，四个筛选条件被错误刷新为“全部”。
- 原因：级联刷新直接清空并重建下拉选项，WPF 绑定会在过程中短暂产生 `null` 选中值；旧逻辑将该临时值误判为失效条件，触发了全部筛选重置。
- 修改方式：先根据候选值校正真正失效的选择，再采用增量删除、插入和移动更新选项集合，避免清空集合造成选中项丢失；刷新期间增加重入保护，并将临时空值安全处理为未选择状态。

## 2026-09-16

### 报文输出窗口与日志架构

- 新增独立、非模态的“报文输出”窗口；从主界面底栏入口打开后，主界面仍可继续操作。
- 新增共享通信日志服务，提供发送与接收完整报文的记录入口，并统一按十六进制显示时间、方向（TX/RX）和数据。
- 窗口关闭后仅释放视图，日志保留在主窗口生命周期内；后续收发管线可调用 `RecordSent`、`RecordReceived` 接入实时记录。
- 报文窗口最小化时会隐藏为主窗口左侧中部的圆形悬浮入口；点击小球可恢复窗口，并会随主窗口移动或调整大小。
- 修正悬浮小球的恢复交互：点击后直接显示并激活报文窗口，无需再点击底栏入口。
- 根据交互调整移除报文窗口最小化及悬浮小球功能，标题栏仅保留最大化/还原和关闭按钮。

## 2026-09-15

### 电机预设位置与本地配置

- 重构电机控制区的目标位置输入：百分比滑条、百分比输入与整数步数输入保持联动，百分比和 `step` 单位采用清晰的分区样式。
- 快捷位置统一命名为 `CLOSE`、`OPEN1`、`OPEN2`、`OPEN3`；三个 OPEN 按钮展示已保存步数及换算百分比。
- 新增基于 MahApps.Metro `Flyout` 的“统一配置”侧栏，可修改三个 OPEN 预设的本地步数并即时刷新主界面摘要。
- 新增软件目录 `config/motor-presets.json` 本地配置文件，默认预设为 3000、6000、12000 step；配置文件缺失或无效时启动自动重建。
- 协议命令尚未接入，目标确认、CLOSE、OPEN 与 STOP 按钮保留但禁用，避免产生已执行设备动作的误导。

### 通信协议记录

- 新增 `PROTOCOL.md`，记录主机发送帧、从机回应帧、帧长度规则与 XOR 校验算法。
- 约定后续实现或调整协议帧处理时，同步维护 `PROTOCOL.md` 中的命令记录。

## 2026-09-01

### 串口设置与连接管理

- 新增右侧模态串口设置面板，支持 COM 口、波特率、数据位、校验位、停止位及读写超时设置。
- 新增系统 COM 口自动识别：设置面板打开时每秒刷新列表，自动移除已拔出的端口，并在仅存在一个可用端口时自动选中。
- 新增串口连接、断开及连接状态同步；顶部状态块和底栏会同步显示连接状态与端口信息。
- 设备物理拔出后自动释放串口并更新为“串口已断开 / 串口已移除”。
- 连接前重新核验已保存 COM 口；连接失败以业务结果反馈，避免拔插或端口占用导致界面异常。
- 接收活跃时通过 `DataReceived` 仅记录活动时间、不读取缓冲区；设置面板关闭后仅在连续 3 秒无接收活动时检查端口状态。
- 调整执行器调试页连接状态布局，使状态灯、状态文字和串口参数对齐，并使用浅灰边框信息块展示。

### 界面圆角与模板稳定性

- 将执行器调试页的卡片、连接状态信息块及提示框统一为轻微圆角。
- 为“读取设备信息”“参数配置”“电机控制”等区域内的操作按钮增加实际圆角模板，并保留悬停、按下和禁用状态反馈。
- 移除存在运行时 XAML 解析风险的共享模板资源，改为页面内样式实现，确保界面可正常加载。
- 优化设备状态区：参数值和故障状态均使用信息框展示，七类故障状态增加绿色状态灯，并提升字体与标题贴合度。
- 在电机控制的目标位置滑条与数值输入框后新增“确定”按钮。

## 2026-09-01

### 初版界面与基础框架

- 创建 WPF + MahApps.Metro 2.4.11 基础框架，并使用 `MetroWindow` 作为主窗口。
- 配置应用主题资源，主窗口默认最大化启动。
- 引入 CommunityToolkit.Mvvm，并按主窗口、视图、视图模型拆分基础结构。
- 建立“执行器调试”页面及左侧 HamburgerMenu 导航壳层。

### 执行器调试界面

- 按设计稿完成静态调试工作台：设备连接概览、读取设备信息、参数配置、电机控制、设备状态、当前位置和底部状态栏。
- 当前仅完成界面展示，尚未接入串口、设备协议、命令或实时数据交互。

### 导航栏体验

- 左侧栏支持展开与收起；收起时保留执行器调试图标、隐藏文字。
- 导航栏采用内联平滑动画，工作区会随侧栏宽度同步移动。
- 收起宽度调整为 35，展开宽度调整为 190。
- 显式将 MahApps 的最小展开宽度设为 35，避免收起动画被默认的 100 宽度限制。

## 2026-09-02

### 顶部品牌标识

- 将公司 logo 加入 MahApps.Metro 顶部标题栏。
- 从 logo 原图中移除白色背景，生成透明版本 `WpfApp1/Resources/logo-transparent.png`。
- 原始 logo `logo.png` 保留在仓库根目录，未被覆盖。
- 左侧导航栏收起宽度进一步调整为 30，顶部按钮和图标列同步缩小。
