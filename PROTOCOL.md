# 通信协议帧记录

本文档是上位机与设备通信的协议帧记录入口。后续新增、修改或实现帧处理功能时，必须同步在本文档登记对应的命令、数据定义和处理状态。

## 1. 主机发送帧格式

```text
AA 55 | CMD | SUB_CMD | LEN | DATA[0] ... DATA[n-1] | CHECKSUM
```

| 字段 | 长度 | 定义 |
| --- | ---: | --- |
| Break | 2 bytes | 固定帧头：`0xAA 0x55` |
| Cmd | 1 byte | 主命令，取值范围：`0x10` ～ `0x7F` |
| Sub Cmd | 1 byte | 子命令，取值范围：`0x00` ～ `0x7F` |
| Data Length | 1 byte | 数据区长度：`LEN`，取值范围 `0x00`～`0xFF` |
| Data | n bytes | 数据区：`DATA[0]` ～ `DATA[n-1]` |
| Checksum | 1 byte | `CMD + SUB_CMD + LEN + DATA` 的 XOR 校验值 |

- 数据区长度：`n = LEN`。
- 整帧长度：`6 + n` 字节。
- 当 `LEN = 0` 时不发送任何 `DATA` 字节，整帧仅包含固定字段和末尾 `CHECKSUM`，长度为 6 字节。
- 示例：读取初始化步数请求无 Data，帧为 `AA 55 10 00 00 10`，共 6 字节。
- 两个帧头字节仅用于识别方向，不参与校验；末尾 `CHECKSUM` 自身不参与校验。

## 2. 从机回应帧格式

```text
55 AA | CMD | SUB_CMD_RESP | LEN | DATA[0] ... DATA[n-1] | CHECKSUM
```

| 字段 | 长度 | 定义 |
| --- | ---: | --- |
| Break | 2 bytes | 固定帧头：`0x55 0xAA` |
| Cmd | 1 byte | 主命令，取值范围：`0x10` ～ `0x7F` |
| Sub Cmd Resp | 1 byte | 支持请求时：`请求 SUB_CMD + 0x40`；不支持请求时：`0xDF` |
| Data Length | 1 byte | 数据区长度：`LEN`，取值范围 `0x00`～`0xFF` |
| Data | n bytes | 数据区：`DATA[0]` ～ `DATA[n-1]` |
| Checksum | 1 byte | `CMD + SUB_CMD_RESP + LEN + DATA` 的 XOR 校验值 |

- 数据区长度：`n = LEN`。
- 整帧长度：`6 + n` 字节。
- 当 `LEN = 0` 时不发送任何 `DATA` 字节，整帧仅包含固定字段和末尾 `CHECKSUM`，长度为 6 字节。
- 两个帧头字节仅用于识别方向，不参与校验；末尾 `CHECKSUM` 自身不参与校验。
- 当响应 `SUB_CMD_RESP = 0xDF` 且帧校验通过时，报文日志类型标记为“不支持”。

## 3. 校验算法

初始校验值为 `0x00`，从 `CMD` 开始依次异或至最后一个数据字节；当 `n = 0` 时，校验范围为 `CMD`、`SUB_CMD` 和 `LEN`。

```c
uint8_t chk = 0;
for (uint16_t i = 0; i < len; i++)
{
    chk ^= p_buf[i];
}
return chk;
```

发送帧中：

- 校验输入从 `CMD` 开始；若直接传入原始帧，则应跳过前 2 个 `Break` 字节。
- `len = 3 + n`，即覆盖 `CMD`、`SUB_CMD`、`LEN` 和全部 `DATA`。
- 计算结果写入帧末尾的 `CHECKSUM`。

## 4. 后续帧登记规范

每新增或修改一项协议帧处理时，在下表增加或更新对应记录，并写明数据区的字节定义、方向和当前实现状态。

| 功能 | 方向 | CMD | SUB_CMD | 数据区定义 | 校验 | 上位机处理状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 通用帧编解码框架 | TX / RX | `0x10` ～ `0x7F` | 请求 `0x00` ～ `0x3F`；响应为请求值 `+ 0x40` 或 `0xDF` | 帧构造、分段接收重组、长度与不包含帧头的 XOR 校验 | XOR（见第 3 节） | 已实现 |
| 读取初始化步数 | TX `0x10 / 0x00`，RX `0x10 / 0x40` | `0x10` | 请求 `0x00`；响应 `0x40` | 无；成功响应 Data 为非空大端无符号 step，长度由 `LEN` 决定（如 `10 20` 为 4128，`10 10 20` 为 1052704） | XOR（见第 3 节） | 已接入并绑定设备状态显示 |
| 读取固件版本 | TX `0x10 / 0x01`，RX `0x10 / 0x41` | `0x10` | 请求 `0x01`；响应 `0x41` | 无；成功响应 Data 为软件主/次/修订版本 3 字节 | XOR（见第 3 节） | 已接入并绑定设备状态显示 |
| 读取硬件版本 | TX `0x10 / 0x02`，RX `0x10 / 0x42` | `0x10` | 请求 `0x02`；响应 `0x42` | 无；成功响应 Data 为硬件版本索引 1 字节 | XOR（见第 3 节） | 已接入并绑定设备状态显示 |
| 读取电压和温度 | TX `0x10 / 0x03`，RX `0x10 / 0x43` | `0x10` | 请求 `0x03`；响应 `0x43` | 无；成功响应 Data 为 `uint16` 大端 mV 和 `int16` 大端 0.1 ℃ | XOR（见第 3 节） | 已接入并绑定设备状态显示 |
| 初始化校准 | TX `0x20 / 0x00` | `0x20` | 请求 `0x00` | 无 Data，帧为 `AA 55 20 00 00 20` | XOR（见第 3 节） | 已接入，仅发送；响应定义待确认 |
| 设置目标位置 | TX `0x20 / 0x01` | `0x20` | 请求 `0x01` | Data 为目标 step 的无符号大端整数，使用容纳该数值的最短字节数；例如 1234 为 `04 D2`，帧为 `AA 55 20 01 02 04 D2 F5` | XOR（见第 3 节） | 已接入，仅发送；响应定义待确认 |
| 紧急停止 | TX `0x20 / 0x02` | `0x20` | 请求 `0x02` | 无 Data，帧为 `AA 55 20 02 00 22` | XOR（见第 3 节） | 已接入，仅发送；响应定义待确认 |
| 读取当前位置 | TX `0x30 / 0x00`，RX `0x30 / 0x40` | `0x30` | 请求 `0x00`；响应 `0x40` | 请求无 Data，帧为 `AA 55 30 00 00 30`；响应 Data 为非空无符号大端 step，长度由 `LEN` 决定，例如 `55 AA 30 40 02 03 E8 99` 表示 1000 step | XOR（见第 3 节） | 已接入，发送后不等待；任意有效响应立即更新当前位置 |

> XOR 以本节为唯一准则：校验范围从 `CMD` 开始，覆盖 `CMD`、`SUB_CMD`、`LEN` 和全部 `DATA`，排除两个帧头字节及末尾 `CHECKSUM` 字节。

## 5. 帧处理规则

1. 接收时根据帧方向确认帧头：主机发送帧为 `0xAA 0x55`，从机回应帧为 `0x55 0xAA`。
2. 读取 `CMD`、子命令和 1 个长度字节后，计算数据区长度 `n`。
3. 收齐 `n` 字节数据和 1 字节校验值后，再进行 XOR 校验。
4. 从机正常回应时，确认 `SUB_CMD_RESP = 请求 SUB_CMD + 0x40`；若值为 `0xDF`，表示该请求不受支持。
5. 只有帧头、长度和校验都正确的帧，才交给对应命令处理逻辑。
6. `0x10` 设备信息读取采用单请求串行策略：同一时刻仅允许一个设备信息读取请求在途；收到匹配的成功响应、不支持响应或超时后，才允许发送下一个请求。`0x30 / 0x00` 读取当前位置不进入等待队列，不创建待响应请求，也不等待超时。
7. 收到 `0xDF`、发送失败、响应超时、帧头/长度/XOR 校验失败时，上位机应显示对应失败提示，不更新对应展示值。`0x20` 动作命令当前没有响应定义，只记录发送成功或失败。
8. 读取请求超时使用串口设置中已应用的“读取超时(ms)”，默认值为 1000 ms；用户修改并应用串口设置后，后续请求使用新值。

9. 报文输出窗口默认跟随最新收发记录自动滚动到最后一行；用户主动向上查看历史记录时保持当前位置，回到底部后恢复自动跟随。
10. 完整但帧头、长度或 XOR 校验不通过的接收帧，必须从正常命令处理链路中丢弃；同时保留原始帧写入报文日志，类型标记为“错误”，不得用于响应匹配或更新设备信息。
11. 报文输出窗口支持按方向、帧类型、Cmd 和 SubCmd 过滤；多个条件同时选择时按“并且”关系筛选，过滤不删除底层日志。四个筛选下拉框均根据其他已选条件联动，只提供当前日志中能够匹配到的选项；条件变化后失效的选择恢复为“全部”。
12. 串口连接状态发生变化时，界面上的初始化步数、退步度数、固件版本、硬件版本、当前电压和当前温度必须先清空；不自动发送读取请求。只有对应的 `0x10 / 0x00`～`0x03` 请求收到有效响应后，才更新对应字段；超时、不支持、发送失败或数据长度错误不得覆盖已有值。退步度数的独立读取命令尚未确认，暂保持为空。
13. 报文日志在内存中默认保留最近 5000 条；报文输出窗口可切换为保留 5000 条或 1 万条。切换为较小上限时，立即淘汰最旧记录；该上限只影响内存中的报文窗口记录，不改变协议帧内容或校验规则。
14. 报文日志新增时，Cmd/SubCmd 筛选项按新增/移除记录增量维护；窗口隐藏时不得执行自动滚动或强制布局，避免高频收发报文阻塞主界面。
15. 清空报文日志时，筛选控件可能短暂产生空选项；上位机必须将其视为未选择并恢复“全部”，不得将空值作为字典键或使窗口退出。
16. 级联筛选更新时，必须先依据候选结果判断当前选择是否真正失效，再更新下拉选项集合；不得通过直接清空集合让有效选项短暂变为 `null`，以免误重置其他筛选条件。选项更新应保留仍然有效的方向、类型、Cmd 和 SubCmd 选择。
17. 长度字段固定为 1 字节，数据区最大长度为 255 字节；`LEN = 0` 时数据区为空且不发送 `DATA` 字节。接收帧的实际长度必须严格等于 `6 + LEN`，长度不匹配的帧按错误帧记录并丢弃。
18. XOR 校验只覆盖 `CMD`、`SUB_CMD`、`LEN` 和全部 `DATA`，不包含两个 `Break` 帧头字节，也不包含末尾 `CHECKSUM` 字节。
19. 收到校验通过且 `SUB_CMD_RESP = 0xDF` 的响应帧时，报文输出窗口类型显示为“不支持”；该帧仍按不支持响应交给请求处理逻辑。
20. 目标位置确认及 CLOSE/OPEN 快捷位置仅在串口已连接且已收到有效初始化步数后可用；可输入 step 范围为 `0` 至初始化步数，百分比按该初始化步数换算。初始化校准和紧急停止仅要求串口已连接。
21. 每次串口连接期间，首次成功读取初始化步数后，上位机自动发送一次 `0x30 / 0x00` 当前位置查询。后续不定时轮询；只要收到校验、长度均有效的 `0x30 / 0x40` 响应帧，即立即更新当前位置 step 和百分比。当前位置读取到有效值后，才允许将其记忆到 `OPEN1`、`OPEN2` 或 `OPEN3`。

## 6. 协议草案待确认项

- `0x30` 的故障与初始化状态子命令在“命令清单”“故障位”“数据结构”三个页签中存在不同定义，尚未绑定任何业务处理。
- `0x10 / 0x00` 的初始化步数响应当前按 `LEN` 指定长度的大端无符号整数处理；“DeviceInfo”数据结构定义为 5 字节版本与装配信息，仍需明确它们是同一命令的不同版本，还是拆分为独立子命令。
