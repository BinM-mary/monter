# 修改记录

## 2026-09-01

### 串口设置与连接管理

- 新增右侧模态串口设置面板，支持 COM 口、波特率、数据位、校验位、停止位及读写超时设置。
- 新增系统 COM 口自动识别：设置面板打开时每秒刷新列表，自动移除已拔出的端口，并在仅存在一个可用端口时自动选中。
- 新增串口连接、断开及连接状态同步；顶部状态块和底栏会同步显示连接状态与端口信息。
- 设备物理拔出后自动释放串口并更新为“串口已断开 / 串口已移除”。
- 连接前重新核验已保存 COM 口；连接失败以业务结果反馈，避免拔插或端口占用导致界面异常。
- 接收活跃时通过 `DataReceived` 仅记录活动时间、不读取缓冲区；设置面板关闭后仅在连续 3 秒无接收活动时检查端口状态。
- 调整执行器调试页连接状态布局，使状态灯、状态文字和串口参数对齐，并使用浅灰边框信息块展示。

### 界面圆角与模板稳定性

- 将执行器调试页的卡片、连接状态信息块及提示框统一为轻微圆角。
- 为“读取设备信息”“参数配置”“电机控制”等区域内的操作按钮增加实际圆角模板，并保留悬停、按下和禁用状态反馈。
- 移除存在运行时 XAML 解析风险的共享模板资源，改为页面内样式实现，确保界面可正常加载。
- 优化设备状态区：参数值和故障状态均使用信息框展示，七类故障状态增加绿色状态灯，并提升字体与标题贴合度。
- 在电机控制的目标位置滑条与数值输入框后新增“确定”按钮。

## 2026-09-01

### 初版界面与基础框架

- 创建 WPF + MahApps.Metro 2.4.11 基础框架，并使用 `MetroWindow` 作为主窗口。
- 配置应用主题资源，主窗口默认最大化启动。
- 引入 CommunityToolkit.Mvvm，并按主窗口、视图、视图模型拆分基础结构。
- 建立“执行器调试”页面及左侧 HamburgerMenu 导航壳层。

### 执行器调试界面

- 按设计稿完成静态调试工作台：设备连接概览、读取设备信息、参数配置、电机控制、设备状态、当前位置和底部状态栏。
- 当前仅完成界面展示，尚未接入串口、设备协议、命令或实时数据交互。

### 导航栏体验

- 左侧栏支持展开与收起；收起时保留执行器调试图标、隐藏文字。
- 导航栏采用内联平滑动画，工作区会随侧栏宽度同步移动。
- 收起宽度调整为 35，展开宽度调整为 190。
- 显式将 MahApps 的最小展开宽度设为 35，避免收起动画被默认的 100 宽度限制。

## 2026-09-02

### 顶部品牌标识

- 将公司 logo 加入 MahApps.Metro 顶部标题栏。
- 从 logo 原图中移除白色背景，生成透明版本 `WpfApp1/Resources/logo-transparent.png`。
- 原始 logo `logo.png` 保留在仓库根目录，未被覆盖。
- 左侧导航栏收起宽度进一步调整为 30，顶部按钮和图标列同步缩小。
